{
  "_comment": "Optional: Cursor → Open User Settings (JSON). KEINE Tokens hier. Für mehrere Repos lieber nur Workspace-.vscode/settings.json nutzen.",
  "terminal.integrated.envFile": "${workspaceFolder}/.env",
  "python.envFile": "${workspaceFolder}/.env",
  "python.terminal.activateEnvironment": true,
  "git.autofetch": true
}
